# R1.9 直接发布候选验证报告

## 发布方式

按用户裁决跳过 RC，不把开发测试工具、环境、LVGL 或网络问题作为本次源码 tag 门禁。验证针对正式源码候选提交 `6c9797fff65709af1cd16258ebd5bd02e706b507`。

## 验证

- Docker `pikadev` 内 `release_check.py --expected-version 1.14.0 --require-clean` 全部通过，无 errors 或 warnings。
- 发布工具单元测试 3/3 通过。
- Release、`PIKA_CONFIG_ENABLE=ON` 的 `pikascript-core` 完整编译通过。
- Release GTest 程序构建通过；递归全局函数查找、递归错误传播、typed-except runtime matrix 共 3/3 通过。
- 编译仅出现仓库既有的未初始化信号和 `strncpy` 警告，不作为本次版本元数据发布阻断项。
