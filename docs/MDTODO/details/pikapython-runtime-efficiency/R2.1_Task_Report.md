# R2.1 任务报告

- 继续检查关键字名称恢复、参数签名拆分、星号参数预扫描和容器访问。
- 常量池关键字查名仍是线性扫描；为其增加缓存会增加常驻 RAM 或对象字段，本轮退出该候选。
- 签名解析仍有重复字符串扫描，但合并解析会扩大语义改动和 Flash 风险，本轮不实施。
- 合格候选为星号预扫描在发现首个 `*` 或 `**` 后立即进入展开，以及复用既有 `n_arg` 字段承载边界信息。
