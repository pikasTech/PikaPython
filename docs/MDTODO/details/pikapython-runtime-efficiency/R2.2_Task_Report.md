# R2.2 任务报告

- 星号参数预扫描由“扫描全部输入并累计数量”改为“发现首个星号立即进入展开”，不增加字段、缓存或缓冲。
- 函数调用继续复用 R1 的单块 scratch，参数上限检查复用既有 `FunctionArgsInfo.n_arg`。
- 新增 `function_call_starred_1000` benchmark；实现未增加 core 的 RAM/Flash，最终默认 `text` 反而减少 3 字节。
