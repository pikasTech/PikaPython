# R2.3 任务报告

- R1 与 R2 使用同一 benchmark 源，三轮交替执行，每轮 7 次重复并取 median。
- `function_call_starred_1000` 的 R2 median 相对 R1 分别约下降 2.9%、20.5%、31.7%。
- 控制项 `while_loop_10000` 同期波动约 -23.6%、-7.3%、+10.7%，且主机负载较高，因此只认定优化方向成立，不声称固定提速比例。
- 默认、minimal 全量隔离回归和 optimize-speed/pool/float/default 相关套件全部通过。
