# R5.4 多配置验证与资源报告

## default

同一隔离源码树和裁剪配置：

| 指标 | 基线 | 当前 | 净变化 |
| --- | ---: | ---: | ---: |
| text | 1072538 | 1072525 | -13 B |
| data | 124248 | 124248 | 0 |
| bss | 24330 | 24330 | 0 |

- 新增定向测试与 Python 3 既有回归：13/13 通过。
- 最终核心宽回归：302/302 通过，覆盖 parser、pikaMain、object、stack、builtin、VM、异常、容器与字符串等 16 个 suites。
- 每个新增 runtime 测试在释放后验证当前堆为零。

## minimal/nano

| 指标 | 基线 | 当前 | 净变化 |
| --- | ---: | ---: | ---: |
| text | 1020207 | 1020154 | -53 B |
| data | 46572 | 46572 | 0 |
| bss | 23066 | 23066 | 0 |

minimal 的 full 异常/parser 测试按 `PIKA_NANO_ENABLE` 配置承诺裁掉；对象、栈和队列核心测试 30/30 通过。最终 `pika_config.h` 已恢复为 default canonical 内容。

## 隔离说明

尝试隔离全量时，移除 LVGL、crypto、网络及部分生成 API 后，`cJSON.module`、若干 module 测试因缺少绑定失败并污染后续 REPL 状态；`string.str_issue1` 在同环境 HEAD parser 基线也失败。上述均作为隔离环境限制记录，不伪装为产品回归，也不计入核心通过集。
