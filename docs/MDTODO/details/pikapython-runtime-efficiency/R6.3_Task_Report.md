# R6.3 性能、回归与资源验收

## 同机 A/B

- 固定 Release、`PIKA_CONFIG_ENABLE=ON`、`pika_config_default.h`、相同预编译字节码负载和 200 万次默认参数函数调用。
- 基线四次：`4.863352s`、`4.891729s`、`4.684342s`、`4.643869s`，均值 `4.770823s`。
- 候选四次：`4.237535s`、`4.149489s`、`4.022640s`、`4.233386s`，均值 `4.160763s`，改善约 `12.8%`。
- 两边均为 `12009057` 次分配、`6007902` 次缓存命中；最终 gprof 中目标函数 self 从 `10.89%` 降至 `2.02%`。

## 回归

- 对象定向测试 20/20 通过。
- `pikaMain`、`builtin`、`vm`、`stack`、`except`、`queue`、`queueObj` 共 207/207 通过。
- 隔离树中的 `datastruct.circlequeue` 被既有 parser 拒绝；同树恢复基线后仍失败，未归因于本轮。
- LVGL、网络、MQTT、requests 和 crypto 非核心测试按任务边界隔离。

## 资源

使用 Release、config ON、统一源码路径和 `size -t` 末行：

- default 基线 `993963/115588/24314`，候选 `993907/115588/24314`，即 `text -56`、`data/bss 0`。
- minimal 基线与候选均为 `941004/43448/23050`，净变化为零。
- 最终 `pika_config.h` 已恢复为 `pika_config_default.h`。
