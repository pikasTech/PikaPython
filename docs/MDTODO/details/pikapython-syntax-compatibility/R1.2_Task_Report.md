# R1.2 任务报告

- 已完整阅读开放 issue：
  - `#347`：默认参数和 `*args` 绑定。
  - `#348`：`**kwargs` 键名显示为哈希数字。
  - `#322`：set literal、嵌套目标解包崩溃和字典迭代顺序。
- 复现：
  - `#347` 的有效 Python 3 形式 `def f(a, b=2, *args)` 在修复前绑定错误。
  - `#348` 在修复前输出 `{'177670': 1, '177671': 2}`。
  - `#322` 的原样例在修复前先触发 `strsAppend(NULL)` 断言，补第一层检查后又暴露空 AST 析构段错误。
- 边界：
  - `def f(a, b=2, c)` 被 CPython 3 拒绝，本轮不接受该语法。
  - set literal 和嵌套目标解包仍不支持，本轮只把崩溃改为语法错误。
  - 字典顺序已由历史提交 `ca2820efb` 修复，实测输出为 `a,b,c`。
