# R1.4 任务报告

- 内核修改：
  - `src/PikaVM.c` 修正默认参数与 `*args` 的分界。
  - `src/PikaVM.c` 恢复 `**kwargs` 的原始关键字名称。
  - `src/PikaParser.c` 在后端、子 AST 和子 ASM 为空时传播语法失败。
- 单元测试：
  - 新增 `fn_default_vars.py`，覆盖普通函数和对象方法。
  - 更新 `stddata.kw_fun`，要求 Python 3 键名。
  - 新增 `parser.issue322_unsupported_nested_unpack`，要求返回 `PIKA_RES_ERR_SYNTAX_ERROR`。
- 约束：
  - 未新增 set literal、嵌套解包或其他语法。
  - 未修改解释器对象模型、字节码格式或总体架构。
