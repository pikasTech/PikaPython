# R1.5 任务报告

- 修复前定向对照：
  - `builtin.fn_default_vars` 失败。
  - `stddata.kw_fun` 失败。
- 修复后定向验证：
  - `builtin.fn_default_vars`、`stddata.kw_fun` 和 `parser.issue322_unsupported_nested_unpack` 共 3/3 通过。
  - `#322` 原文件入口输出 `Syntax error`，不再断言或段错误。
  - 字典顺序样例输出 `a : 1`、`b : 4`、`c : 7`。
- 最终五档回归：
  - minimal 为 110/110。
  - optimize-speed、pool、float、default 各为 205/205。
- CPython 3 对照：
  - 新增默认参数测试输出 `PASS`。
  - 非默认参数位于默认参数之后会被 CPython 3 直接拒绝。
