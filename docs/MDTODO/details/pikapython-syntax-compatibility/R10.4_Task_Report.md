# R10.4 任务报告

- R6/R8/R10 及合法邻接定向：6/6 通过。
- default 隔离全量：400/400 通过。
- minimal 隔离全量：180/180 通过；nano 裁掉 parser GTest，故语法矩阵由 default 覆盖。
- default `TOTALS`：text 1072599、data 125784、bss 24338，相对 R9 text -496 B。
- minimal `TOTALS`：text 1022741、data 47180、bss 23074，相对 R9 text -512 B。
- default 配置已恢复，`git diff --check` 通过。

LVGL、网络、hashlib、mqtt、requests、jrpc 按任务范围隔离。
