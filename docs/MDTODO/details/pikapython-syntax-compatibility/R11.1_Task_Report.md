# R11.1 任务报告

建立两组差分矩阵：33 个 CPython 3 编译期 `SyntaxError` 候选，以及 20 个可解析语法的运行语义候选。计数按独立语法合同/独立阶段行为，不把同一默认参数根因的三个表现重复计数。

解析矩阵结果：27 项被错误接受、2 项触发 SIGSEGV、4 项正确返回 `PIKA_RES_ERR_SYNTAX_ERROR`。运行矩阵结果：9 个独立合同不兼容，11 项行为符合预期或未形成独立问题。所有探针在隔离 Linux 测试树运行，正式测试文件未修改。
