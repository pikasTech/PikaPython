# R11.4 任务报告

本轮达到“20 个独立语法处理错误”目标：11 个解析阶段问题、9 个运行阶段问题。另有 16 个解析错误接受作为附加发现，不计入模板数量。

优先级建议：

- P0：空 `import` 与 `from x import` 的 `Suger_import()` SIGSEGV。
- P1：`and`/`or` 不短路、默认参数每次调用求值、解包越界错误合同。
- P1：孤立 block clause 与循环/函数外控制语句被错误接受。
- P2：幂运算结合性、推导式作用域、未绑定局部变量和星号解包。
- P2：附加 16 项 parser 错误接受按共同 token/block 根因分批处理。

诊断探针仅存在于隔离测试树，结束前已用正式 `py-test.cpp` 覆盖并重建。正式 R6/R8/R10 定向回归 6/6 通过，`git diff --check` 通过。
