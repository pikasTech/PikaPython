# R12.1 任务报告

修复空 `import` 与 `from module import` 缺目标时的解析崩溃。根因是语法糖展开在空字符串上执行 `strlen(...) - 1` 写入；现于 sugar 展开前的既有 `Lexer_isError()` 中识别空 import 目标，统一返回 `PIKA_RES_ERR_SYNTAX_ERROR`，不改变 parser 架构、不新增缓冲，也不影响 VM/runtime 热路径。

新增两条表驱动 Linux GTest：两种崩溃输入必须返回语法错误；普通 import、带 alias 的 import、from-import 及带 alias 的 from-import 必须继续成功。隔离 LVGL 和可选依赖后同步 core 并增量重建，连同 R6/R8/R10 parser 回归共 6/6 通过。
