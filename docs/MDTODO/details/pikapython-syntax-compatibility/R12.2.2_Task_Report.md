# R12.2.2 任务报告

修复附加候选中的 10 个声明和目标错误接受：模块级 `nonlocal`、空 `assert`、删除 literal、非法 def/class 名、非法 for 目标、`**kwargs` 后继续声明参数、重复 varargs、tuple 增强赋值和 literal 注解目标。

实现复用现有 `Lexer_matchStmtType()` 约束名称及 for/del/注解目标；增强赋值仅允许单一可写目标；`_defGetDefault()` 增加两个局部标志识别 varargs/kwargs 顺序。没有新增 AST 节点、对象字段或 VM/runtime 分支。

全部样例先经 CPython 3 确认为 `SyntaxError`。隔离 Linux 树同步 core 并重建后，新增非法矩阵与合法名称/参数/目标邻居通过，完整 parser 测试 13/13 通过。
