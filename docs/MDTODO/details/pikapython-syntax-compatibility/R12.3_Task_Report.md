# R12.3 任务报告

## 结果

R12.3 分三批完成 R11 已确认的运行语义修复：

- R12.3.1：`and/or` 短路求值和幂运算右结合。
- R12.3.2：默认参数定义时求值、可变默认值复用，以及未绑定局部变量的 `UnboundLocalError`。
- R12.3.3：推导式目标作用域、星号解包和定长解包数量/`ValueError` 合同。

全部改动保持 parser/ASM/VM 现有架构，不新增 VM 指令或 frame 常驻字段；VM/runtime 热路径优先级未让位于 parser 性能。

## 验收

各子任务均有独立单元测试与报告。最终状态：

- default 本轮语言回归 `parser.*:pikaMain.python3_*` 23/23 通过，相关循环 4/4 通过。
- minimal 隔离全量 183/183 通过。
- 当前 core：default `text=1080717 data=125784 bss=24338`；minimal `text=1028379 data=47180 bss=23074`。
- default 的正确性阶段 text 增量和函数定义/调用动态成本由 R12.4 独立测量、优化与验收，语义和错误合同不得回退。
