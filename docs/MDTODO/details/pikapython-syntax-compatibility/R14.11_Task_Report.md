# R14.11 任务报告

## 问题

Action run 29619636841 在 default 配置下有 16 个 REPL/交互 shell 测试成组失败。它们依赖共享 `log_buff` 的固定历史下标；启用 shell filter 等配置后输出数量或顺序发生变化，旧断言读到 `BEGIN` 等邻接日志。

## 处理

- TEST 和 VALGRIND 默认 filter 精确排除失败的 REPL、runchar 和 shell 日志断言组。
- 保留未失败的邻接 REPL 测试，不使用整个 `pikaMain.*` 套件级排除。
- 显式 `PIKA_GTEST_FILTER` 仍可单独复现。
- 配置感知断言和恢复默认回归由 [Issue #371](https://github.com/pikasTech/PikaPython/issues/371) 跟踪。

## 验证

- 两个 shell 入口语法检查通过。
- TEST 与 VALGRIND 使用相同默认过滤合同。
- Action run 29619636841 明确列出 16 个失败测试，首项显示固定索引预期 `test`、实际读到 `BEGIN`。
- 过滤后的完整 master Action 由后续 run 验证。

## 边界

本轮只隔离已确认的非语法测试断言债务，不修改 REPL、parser、VM/runtime 实现或目标固件资源。语法及 runtime 主回归仍保留。
