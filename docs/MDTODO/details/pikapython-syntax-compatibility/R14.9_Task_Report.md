# R14.9 任务报告

## 问题

Action run 29618450789 的 TEST、Lcov、VALGRIND 和 BENCHMARK 均已完成，最终在 benchmark 结果发布阶段失败。Linux 构建覆盖了仓库内 tracked 生成物，发布 Action 切换到 gh-pages 时被脏工作树拒绝。

## 修复

- benchmark 完成后先把本轮 `benchmark_result.json` 复制到 runner 临时目录。
- 恢复仓库内全部 tracked 工作树改动，隔离构建对已跟踪生成物和配置的覆盖。
- 发布 Action 改从 runner 临时目录读取结果，避免恢复工作树时回退本轮基准数据。
- 删除原来只移除 Linux shell 脚本、却不能清理 tracked 构建改动的 CLEAR 行为。

## 验证

- CI workflow 可由 YAML 解析器正常加载。
- `git diff --check` 通过。
- Action run 29618450789 证明 TEST、Lcov、VALGRIND、BENCHMARK 已通过，并给出两个 tracked 生成物阻塞分支切换的明确证据。
- 修复后的完整发布链由后续 master Action 验证。

## 边界

本轮只修复 CI 构建产物隔离与 benchmark 结果交接，不改变 benchmark 内容、parser、VM/runtime 语义或目标固件资源占用。
