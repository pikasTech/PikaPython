# R2.2 任务报告

- 参数绑定判断改为把默认参数计入可接受的位置参数范围，避免默认参数被错误装入 `*args`。
- 关键字名称从当前字节码常量池恢复，普通关键字字典使用源码名称而不是哈希数字；动态 `**dict` 键保留哈希回退。
- 新增 `fn_kwargs_required.py`、`fn_default_vars.py`、`fn_kwargs_order.py`，覆盖 #349、默认参数与 `*args` 混用及 kwargs 顺序。
