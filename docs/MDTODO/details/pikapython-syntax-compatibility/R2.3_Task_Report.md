# R2.3 任务报告

- `stack_pushSize()` 改为写入前比较元数据指针与已分配尾界，避免越界写后才报告。
- 签名解析复用既有 `n_arg` 字段保存原始绑定槽位数；超过 `PIKA_ARG_NUM_MAX` 时设置可捕获的 `OverflowError: arg limit`，不再覆盖固定 `argv` 暂存区。
- 新增 `stack.size_array_capacity` 和 `fn_too_many_bound_args`；后者在 Python 层捕获异常并输出 PASS，验证错误可见且不会污染后续 VM 测试。
