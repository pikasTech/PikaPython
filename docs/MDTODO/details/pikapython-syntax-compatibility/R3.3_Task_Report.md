# R3.3 任务报告

## 隔离范围

- 在 NC01 独立临时源码和构建目录执行。
- 隔离 LVGL、hashlib、network、socket、mqtt、requests、jrpc 及其他非语法依赖。
- Linux 生成 core 与根目录 `src/` 同步后构建。

## 回归结果

- 定向参数、栈和 parser：7/7。
- default 隔离全量：394/394。
- minimal 隔离全量：179/179。
- optimize-speed、pool、float、default 相关套件：各 74/74。
- 一次配置切换后立即运行 default 时，16 个非语法 REPL 输入钩子测试受全局测试状态影响失败；相同最终二进制独立复跑为 394/394，语法、VM 和参数测试均未出现失败。

## 资源

- R2 默认门槛：`text=1072194 data=125784 bss=24338`。
- R3 默认：`text=1072106 data=125784 bss=24338`，text 减少 88 字节。
- R2 minimal 门槛：`text=1022658 data=47180 bss=23074`。
- R3 minimal：`text=1022602 data=47180 bss=23074`，text 减少 56 字节。
- RAM/Flash 均未增加。
