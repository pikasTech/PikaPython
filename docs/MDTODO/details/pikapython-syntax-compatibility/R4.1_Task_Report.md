# R4.1 任务报告

## 复现结论

- 按 #356 原样建立三个顶层函数，覆盖函数定义后直接取函数对象、赋给别名以及通过别名调用。
- Linux default 配置定向测试 1/1 通过，输出为 `blink stat timer`。
- Linux minimal/nano 配置定向测试 1/1 通过，函数定义顺序和连续别名赋值均未触发 `NameError`。
- 当前主线无法复现 issue 报告版本 1.11.8/1.13.4 的偶发故障，因此保留回归测试，不制造无证据的内核补丁。

## 边界

issue 同时提到 128 KiB MCU 固件体积，但 Linux 静态库尺寸不能直接等同 MCU 最终链接尺寸。本轮只验证语法和符号引用，不开展 ARM/Keil 或板级体积调查。
