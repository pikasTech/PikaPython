# R4.3 任务报告

## 验收结果

- minimal/nano：`issue.issue356_top_level_function_refs` 1/1 通过；core `TOTALS` 为 `text=1022602 data=47180 bss=23074`。
- default：同一测试 1/1 通过；core `TOTALS` 为 `text=1072106 data=125784 bss=24338`。
- 两组尺寸与 R3 门槛逐项一致，新增测试不进入 core，没有 RAM/Flash 增长。
- 构建在隔离临时源码中进行；未加载 LVGL 子模块，未执行网络、hashlib 或其他非语法测试。
