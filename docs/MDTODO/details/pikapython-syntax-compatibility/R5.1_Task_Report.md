# R5.1 任务报告

## 复现结论

- #328 正文和评论均为空，标题指向 C 模块 API 的 keyword/非 keyword 混用问题。
- 现有原生 `add(val1,val2)` 可稳定覆盖纯位置、位置加 keyword、乱序全 keyword 和非法 keyword。
- 合法三种调用均返回 3；缺少必选参数可捕获。
- 修复前传入未知 keyword 会在 `_kw_to_pos_all()` 对缺失形参执行 `pika_assert(pos_arg != NULL)`，Linux 进程直接 `Aborted`。
