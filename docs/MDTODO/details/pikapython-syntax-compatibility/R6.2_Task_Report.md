# R6.2 任务报告

- `Lexer_isError()` 直接复用第一次词法分析生成的 token 流。
- 单次扫描拒绝以下结构：
  - 开括号后立即出现逗号；
  - 连续逗号；
  - operator 后缺少操作数；
  - 字典冒号后缺少值。
- block parser 拒绝空 `if/while/elif` 条件。
- `for` parser 拒绝缺目标或缺 iterable。
- 新增 15 项非法输入表驱动回归和合法邻接反例。
- 变更不增加语法能力，不改变 parser/VM 架构。
