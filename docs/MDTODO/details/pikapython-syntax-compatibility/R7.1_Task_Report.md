# R7.1 任务报告

## 资源基线

- R6 default core：`1072239 / 125784 / 24338`。
- R6 minimal core：`1023005 / 47180 / 23074`。
- default 三项定向语法回归：
  - 分配次数：`9222`；
  - 缓存命中：`8053`；
  - 本轮未增加分配。

## 性能优先级

- 性能门禁以 VM/runtime 为主。
- 本轮候选仅修改 parser 关键字识别。
- 未触碰 VM、对象模型、参数绑定和字节码执行热路径。
- parser 编译性能仅作辅助判断，不为 parser 微基准牺牲 runtime 性能。
