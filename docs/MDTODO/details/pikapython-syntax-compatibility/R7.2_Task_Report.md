# R7.2 任务报告

- 为 `while`、`if`、`elif` 关键字表保存编译期长度。
- 删除每行解析时对应的 `strGetSize()` 扫描。
- `break`、`continue` 的固定长度改为编译期表达式。
- 改动仅位于 `parser_line2Ast()` 的关键字识别路径：
  - 不改变 parser/VM 架构；
  - 不扩大语法范围；
  - 保持 R6 的 15 项非法语法拒绝合同；
  - 保持相邻合法语法合同。
