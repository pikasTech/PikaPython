# R7.3 任务报告

## 回归

- default 定向语法回归：3/3 通过。
- default 隔离全量：397/397。
- minimal 同口径隔离全量：180/180。
- 隔离项：LVGL、hashlib、network、socket、mqtt、requests、jrpc。

## 资源

- default core：`1072207 / 125784 / 24338`。
  - 相对 R6 回收 32 字节 text；
  - data/bss 不变；
  - 相对 R6 前基线仍有 323 字节 text 待回收。
- minimal core：`1022957 / 47180 / 23074`。
  - 相对 R6 回收 48 字节 text；
  - data/bss 不变；
  - 相对 R6 前基线仍有 355 字节 text 待回收。

## 性能

- VM/runtime 预编译字节码烟测 CPU 中位数：
  - `function_call_1000`：7.27 ms；
  - `function_call_starred_1000`：14.3 ms；
  - `for_print_1000`：15.3 ms；
  - `prime_number_100`：1.45 ms。
- 主机负载较高且为 Debug 构建，数值只作烟测。
- VM/runtime 源码和字节码执行热路径未修改。
- 不以 parser 指标换取 runtime 退化。

## 异常复核

- 首次组合基准在首项后瞬态退出。
- 两项最小组合随后复测退出码为 0，未稳定复现。
- 四项独立进程均完成，不登记独立回归问题。
- 首次 default 全量受此前错误工作目录测试状态影响：
  - 16 项 REPL/shell 首次失败；
  - 16 项定向随后全部通过；
  - `pikaMain.*` 143 项全部通过；
  - 最终完整 397 项全部通过；
  - 未形成稳定回归。
