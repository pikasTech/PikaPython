# R8.1 任务报告

- 读取并复现已有 GitHub #367。
- 初始 parser 错误接受 5 类 Python 3 非法语法：
  - 默认参数后出现必选参数；
  - 重复形参；
  - keyword 实参后出现位置实参；
  - 同一次调用重复 keyword；
  - 字典项缺少冒号。
- 增加 3 个嵌套变体：
  - 嵌套调用中的位置实参顺序错误；
  - 嵌套调用中的重复 keyword；
  - 列表内嵌字典缺少冒号。
- 增加 5 类合法邻接语法，初始均可编译。
