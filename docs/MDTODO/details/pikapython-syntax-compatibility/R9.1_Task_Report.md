# R9.1 任务报告

## 结论

对象级对比确认 R8 增量集中在 `PikaParser.o`：default 从 62260 B 增至 63305 B（+1045 B），minimal 从 49801 B 增至 50126 B（+325 B）。主要增量符号为 default 的 `AST_parseStmt`（+463 B）、`_defGetDefault`（+395 B）、`parser_line2Ast`（+150 B），以及 minimal 的 `AST_parseStmt`（+235 B）、`parser_line2Ast`（+48 B）。

## 判定

R8 新增的关键字/形参集合只存在于 parser 路径，没有进入 VM/runtime 热路径。尝试把 `keywordNames` 收窄到方法分支后，x86_64 C 栈帧仍为 0x108 B 且 text 反增 16 B，因此不保留该试验。
