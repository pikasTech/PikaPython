# R9.3 任务报告

## 验证

- R6/R8 语法定向：5/5 通过，覆盖 malformed、五类 #367 语义错误、嵌套变体及合法邻接。
- default 隔离全量：399/399 通过；过滤 LVGL、hashlib、network、socket、mqtt、requests、jrpc。
- minimal 隔离全量：180/180 通过；过滤范围相同。
- default `TOTALS`：text 1073095、data 125784、bss 24338。
- minimal `TOTALS`：text 1023253、data 47180、bss 23074。
- `git diff --check` 通过。

首次 default 全量从错误工作目录启动，因测试资产相对路径缺失而超时；改从隔离源码 `port/linux` 重跑后 399/399，通过结果以上述有效运行计。
